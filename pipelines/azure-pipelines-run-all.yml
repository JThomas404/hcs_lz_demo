# trigger: none

# stages:
#   - stage: Disabled
#     displayName: "Run-all disabled"
#     jobs:
#       - job: explain
#         displayName: "Explain supported execution model"
#         pool:
#           vmImage: "ubuntu-latest"
#         steps:
#           - script: |
#               set -euo pipefail
#               echo "azure-pipelines-run-all.yml is intentionally disabled."
#               echo "Use per-stack pipelines with explicit stack_path values instead."
#               echo "Reference: pipelines/azure-pipelines-per-stack.yml and pipelines/azure-pipelines-stack-wrapper.yml"
#             displayName: "Run-all disabled by design"
